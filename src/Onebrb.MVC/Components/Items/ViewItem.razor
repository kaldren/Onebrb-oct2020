@using Onebrb.MVC.Models.Item
 

    <div id="item-profile">
        <p>@Item.Title</p>

        <p>Rating: @Item.Ratings.Where(x => x.Value > 0).Count() | @Item.Ratings.Where(x => x.Value > 0).Count()</p>

        <div class="item-comments">
            Comments widget
        </div>
    </div>

@code {
    [Parameter]
    public ItemViewModel Item { get; set; }
}
