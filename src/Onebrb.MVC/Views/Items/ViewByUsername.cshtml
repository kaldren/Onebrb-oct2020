@model ICollection<Onebrb.MVC.Models.Item.ItemViewModel>

<component type="typeof(Onebrb.MVC.Components.Items.ViewItemsByUsername)" param-Items="Model" param-ViewerId="@this.User?.Claims?.FirstOrDefault(x => x.Type == "Id")?.Value" render-mode="ServerPrerendered" />

@{
}

@section Scripts {
    <script src="_framework/blazor.server.js"></script>
}