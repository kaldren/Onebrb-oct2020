@using Onebrb.MVC.Components.Items
@model Onebrb.MVC.Models.Item.EditItemViewModel

@if (!User.Identity.IsAuthenticated)
{
    return;
}

@if (User.Identity.IsAuthenticated)
{
    <component type="typeof(Onebrb.MVC.Components.Items.EditItem)" param-Item="Model" render-mode="ServerPrerendered" />
}

@{
}

@section Scripts {
    <script src="_framework/blazor.server.js"></script>
}
