@using Onebrb.MVC.Components.Items
@model Onebrb.MVC.Models.Item.EditItemViewModel
@*<component type="typeof(Onebrb.MVC.Components.Items.ViewItem)" param-Item="Model" render-mode="ServerPrerendered" />*@

@if (!User.Identity.IsAuthenticated)
{
    return;
}

@if (User.Identity.IsAuthenticated)
{
    string currentUserId = this.User.Claims.FirstOrDefault(x => x.Type == "Id").Value;

    //var claims = this.User.Claims;

    //// Only the author of the product is allowed to edit it
    //if (currentUserId != Model.UserId)
    //{
    //    return;
    //}

    <component type="typeof(Onebrb.MVC.Components.Items.EditItem)" param-Item="Model" param-CurrentUserId="currentUserId" render-mode="ServerPrerendered" />
}

@{
}

@section Scripts {
    <script src="_framework/blazor.server.js"></script>
}
